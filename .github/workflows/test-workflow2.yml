name: test workflow 2
on:
  workflow_call:
  
jobs:
  print_text:
    runs-on: Linux
    steps:
    - name: Checkout 
      uses: actions/checkout@v2
      with:
        ref: ${{ github.head_ref }}
        
    - name: Run ls
      run: echo "${{ toJSON(github.event.pull_request.labels.*.name) }}"
