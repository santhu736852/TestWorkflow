name: CI UI

on:
  workflow_call:

permissions: write-all

jobs:
  call-workflow1:
    if: ${{ needs.filter-path.outputs.ui == 'true' }}
    uses: ./.github/workflows/test-workflow2.yml
    secrets: inherit